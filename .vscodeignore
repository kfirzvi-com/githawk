# Everything not needed at runtime. Verified with `vsce ls`.
.vscode/**
.vscode-test/**
.github/**
node_modules/**
src/**
tests/**
artifacts/**
playwright-report/**
test-results/**

# Build + tooling config
esbuild.js
vite.config.ts
playwright.config.ts
svelte.config.js
eslint.config.mjs
**/tsconfig*.json

# Sources and maps
**/*.ts
dist/**/*.map

*.vsix

# Repo housekeeping
.gitignore
