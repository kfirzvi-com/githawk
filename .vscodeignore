# Everything not needed at runtime. Verified with `vsce ls`.
.vscode/**
.vscode-test/**
.github/**
node_modules/**
src/**
tests/**
scripts/**
artifacts/**
playwright-report/**
test-results/**

# Build + tooling config
esbuild.js
vite.config.ts
playwright.config.ts
svelte.config.js
eslint.config.mjs
**/tsconfig*.json

# Sources and maps
**/*.ts
dist/**/*.map

*.vsix

# Icon master — only the 256px media/icon.png ships
media/icon-1024.png

# Repo housekeeping
.gitignore
